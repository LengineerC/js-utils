# omit

## Function - omit

Exclude object key-value pairs by key names, similar to TypeScript's Omit

### Parameters

- `obj`: `object` - Source object

- `keys`: `string[]` - Array of key names to exclude

### Return Value

`object` - Object after excluding the specified key names

