# pick

## Function - pick

Extract object key-value pairs by key names, similar to TypeScript's Pick

### Parameters

- `obj`: `object` - Source object

- `keys`: `string[]` - Array of key names to extract

### Return Value

`object` - Object extracted from the specified key names

